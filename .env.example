# Optional: email alerts (circuit breaker / completion). Leave unset to skip.
# SMTP_HOST=
# SMTP_PORT=465
# SMTP_USER=
# SMTP_PASSWORD=
# NOTIFY_TO=
# NOTIFY_FROM=

# This public export runs offline by default (fixtures under data/sample/).
# Live HTTP integration is not shipped in this repository; see README.
